# 2026-09-16 — Remaining 3-key drift, full ES parity (ut-docs#2245)

## What shipped

Translated the last 3 core keys not yet covered by this pack
(`plugins.manage.update_unknown`, `plugins.manage.update_unknown_hint`,
`settings.shop_type.service_layout_note`), removed their now-stale
entries from `i18n-baseline/es.untranslated.txt` (now empty), bumped
`manifest.json`'s version `1.1.67` → `1.1.68`, and updated the
manifest description from "translates a growing subset..." to "full
coverage..." to reflect the newly-reached 2415/2415 parity.

Card `ut-docs#2245` originally covered 26 `diagnostics.*`/
`settings.diagnostics.*` keys too, but those had already been landed
by a prior cycle (`ut-plugin-language-es#269`, ut-docs#2264) before
this cycle picked the card up — verified via a fresh
`check-key-drift.sh` run against `origin/main`'s current `en.json`
before starting any translation work, which found only these 3 keys
outstanding, not 26/29 as the card's older comments described.

## Independent review

Sonnet subagent, fresh context (card is `complexity:easy`). Verdict:
**one real, fixable issue found — fixed, then safe to merge.**

Findings:
- **Fixed:** `plugins.manage.update_unknown_hint` originally translated
  "marketplace" as "el catálogo del **mercado**". Every other
  occurrence of "marketplace" in this file (14+ places, including the
  neighboring `plugins.import.*`/`plugins.install.error.*`/
  `plugins.store.empty`/`plugins.trust.unverified_hint`) keeps it as
  the untranslated loanword "marketplace" — and "mercado" already
  means something else in this file (a geographic market / market
  stall, e.g. `setup.shop_type.market_stall`). Changed to "el catálogo
  del **marketplace**..." to match established usage. Re-verified:
  JSON still valid and sorted (2415 keys), `check-key-drift.sh` and
  `validate.sh` both green after the fix, commit author correct.
- The other two strings (`update_unknown`, `service_layout_note`) were
  judged natural and terminology-consistent against existing keys
  (`Artículos` matches `kitchenstations.items.title`/general usage,
  `Estaciones de cocina` matches `kitchenstations.title`, `Mesas`
  matches `tables.title`, `Fichas de menú ocultas` matches
  `menulayout.title`, `Servicios` matches `setup.shop_type.service`).

What it verified, beyond reading the diff:
- `scripts/validate.sh` and `scripts/check-key-drift.sh` (with
  `UT_CORE_EN_JSON` pointed at a local `universal-till` checkout) both
  run clean: 2415/2415 keys, 0 drift, 0 orphans, 0 empty values, 0
  untranslated-present, 0 token mismatches — both before and after the
  terminology fix.
- `scripts/check-version-bump.sh` confirms the bump and correctly
  attributes it to `locales/es.json` + `manifest.json`.
- JSON parses and remains fully alphabetically sorted (2415 keys).
- Diff scope is exactly `locales/es.json`, `manifest.json`,
  `i18n-baseline/es.untranslated.txt` — nothing else touched.
- Commit author on all 3 commits (initial translation + terminology
  fix) is `Farshid Mirza <farsid@taskrunnertech.co.uk>` — not an AI
  identity.

## Verified beyond automated tests

- `scripts/validate.sh`, `scripts/check-key-drift.sh` run locally
  against `origin/main`'s `en.json`, green before the fix (3 keys
  landed, one word wrong) and green after (fixed).

## Safe to merge

Yes. Merge via `merge_method: "merge"` (never squash/rebase — see
`reviewer` skill's "Merge method" note, ut-docs#250).

## Deferred / out of scope

- Companion PR in `ut-plugin-language-de` covers the German half of
  the same card (same 3 keys, no fix needed there).
- `ut-docs#2245`'s original acceptance criteria mentioned 26 keys total
  (from an earlier scope-check comment); those were already delivered
  by a separate, prior PR (`#269`/ut-docs#2264) before this cycle
  started — not re-verified line-by-line here beyond confirming the
  drift check reports full parity.

PR: (opened this cycle, see linked ut-docs#2245)
