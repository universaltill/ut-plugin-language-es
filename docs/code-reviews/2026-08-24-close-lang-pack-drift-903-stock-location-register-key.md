# lang-pack-drift red — 1 missing key (permissions.action.stock_location_management) — ut-docs#903

**Reviewer**: self-reviewed (mechanical translation-sync follow-up to an
already-independently-reviewed core change, matching this pack's own
closed precedents for prior lang-pack-drift fixes).
**Branch**: `sync/903-stock-location-register-permission-keys` (base: `main`).
**Date**: 2026-08-24.

## What shipped

`lang-pack-drift` went red on `universal-till`'s `main` (push run
`32713522564`) after merging universal-till#481 (ut-docs#903, a dedicated
`stock_location_management` permission action, reviewed and merged in the
same pipeline cycle), which added one new core key to
`web/locales/en.json`: `permissions.action.stock_location_management`
("Locations & registers").

This was **not** caught in advisory mode on the PR itself — the PR's own
`lang-pack-drift` job is advisory-only by design (per `universal-till/
CLAUDE.md`), and turned blocking only once merged to `main`.

This PR adds the Spanish translation ("Ubicaciones y cajas") to
`locales/es.json`, appended at the end (this pack's existing insertion-
order convention — not alphabetical, unlike `ut-plugin-language-de`).
This pack is a partial-coverage pack (ratchet mode, not exact parity —
952 core keys remain in `i18n-baseline/es.untranslated.txt`), so no
baseline edit was needed: the key is brand new to core (never previously
in the baseline as known-untranslated debt), so translating it directly
is a pure addition, not a baseline prune.

A companion PR in `ut-plugin-language-de` carries the matching German
translation — both are required to fully close the drift; neither alone
closes it.

## Verified beyond automated tests

- `python3 -c "import json; json.load(open('locales/es.json'))"` — valid JSON.
- `bash scripts/validate.sh` — `ok com.universaltill.language-es v1.1.3 (es)`.
- `UT_CORE_EN_JSON=<local checkout of universal-till's merged en.json,
  commit c482c0a> bash scripts/check-key-drift.sh` — `ok -- 628/1580 core
  keys translated, 952 known-untranslated (baseline), 8 known-same-as-
  English (allowlist), 0 drift, 0 orphans, 0 empty values, 0
  untranslated-present`.
- The new value carries no `%s`/format-verb placeholder, so no token-count
  cross-check applies.

## Outcome

Merging via this PR closes `universal-till`'s `lang-pack-drift` red on
`main` once combined with the companion `ut-plugin-language-de` PR. Both
verified green against the merged core `en.json` before push.

---
_Generated by [Claude Code](https://claude.ai/code)_
