# lang-pack-drift red — 2 missing keys (catalog.barcode.force_plain, catalog.barcode.force_plain_hint) — ut-docs#948

**Reviewer**: self-reviewed (mechanical translation-sync follow-up to an
already-independently-reviewed core change, matching the standing pattern
for this check — see the companion `ut-plugin-language-de` PR closing the
same drift, and prior closed precedents #925, #903, #862, #910, #746,
#891, #665, #612, #494, #374, #441, #579, #296 in that pack).
**Branch**: `sync/948-barcode-plain-escape-hatch-keys` (base: `main`).
**Date**: 2026-08-24.

## What shipped

`lang-pack-drift` went red on `universal-till`'s `main` (push run
`32768812639`) after merging universal-till#494 (ut-docs#948, the catalog
barcode plain-code escape hatch), which added two new core keys to
`web/locales/en.json`:

- `catalog.barcode.force_plain` — "Plain code (ignore weight/price)"
- `catalog.barcode.force_plain_hint` — "Treat this code as a plain
  product barcode, even if weight/price-embedded barcodes are enabled for
  this shop."

This was **not** caught in advisory mode on the PR itself — the PR's own
`lang-pack-drift` job is advisory-only by design (per `universal-till/
CLAUDE.md`), and turned blocking only once merged to `main`.

This pack carries substantial pre-existing baseline debt on the
surrounding `catalog.*` keys (`catalog.barcode`, `catalog.barcodes`, and
the rest of that cluster are not yet translated at all — tracked in
`i18n-baseline/es.untranslated.txt`), so there was no existing
`catalog.barcode*` neighbourhood to insert alongside. This PR adds the
two new keys directly to `locales/es.json` (next to the existing
`catalog.error.*` cluster, the nearest `catalog.*` block actually
present in this file) without touching the unrelated pre-existing debt:

- `catalog.barcode.force_plain`: "Código simple (ignorar peso/precio)"
- `catalog.barcode.force_plain_hint`: "Tratar este código como un código
  de barras de producto simple, aunque este comercio tenga activados
  códigos de barras con peso/precio incorporado."

No baseline edit needed — both keys previously didn't exist in core, so
this is a pure addition; `i18n-baseline/es.untranslated.txt` stays at its
existing 973 entries (unrelated pre-existing debt), keeping this pack's
0-new-drift state.

A companion PR in `ut-plugin-language-de` carries the matching German
translation — both are required to fully close the drift; neither alone
closes it.

## Verified beyond automated tests

- `python3 -c "import json; json.load(open('locales/es.json'))"` — valid
  JSON.
- `bash scripts/validate.sh` — `ok com.universaltill.language-es v1.1.7
  (es)`.
- `UT_CORE_EN_JSON=<local checkout of universal-till's merged en.json,
  commit 8a1b636f22eaaff7dbd668c4ee9d231d9f044462> bash
  scripts/check-key-drift.sh` — `ok -- 629/1602 core keys translated, 973
  known-untranslated (baseline), 8 known-same-as-English (allowlist), 0
  drift, 0 orphans, 0 empty values, 0 untranslated-present`.
- Neither new value carries a `%s`/format-verb placeholder, so no
  token-count cross-check applies beyond the guard's own 0-mismatch
  report.

## Outcome

Merging via this PR closes `universal-till`'s `lang-pack-drift` red on
`main` once combined with the companion `ut-plugin-language-de` PR. Both
verified green against the merged core `en.json` before push.

---
_Generated by [Claude Code](https://claude.ai/code)_
